steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build' 
      - '-t' 
      - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:$BUILD_ID'
      - '-t'
      - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:latest'
      - '.'
    dir: 'SADP/services/poml-orchestrator'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push' 
      - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:$BUILD_ID'

  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push' 
      - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:latest'

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'sadp-poml-orchestrator'
      - '--image'
      - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:$BUILD_ID'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--set-env-vars'
      - 'NODE_ENV=production,GCP_PROJECT_ID=$PROJECT_ID,LEARNING_PIPELINE_URL=https://sadp-learning-pipeline-355881591332.us-central1.run.app,OPTIMIZATION_SERVICE_URL=https://sadp-prompt-optimization-355881591332.us-central1.run.app'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '2'
      - '--max-instances'
      - '10'
      - '--timeout'
      - '300'

images:
  - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:$BUILD_ID'
  - 'gcr.io/$PROJECT_ID/sadp-poml-orchestrator:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: 1200s